#ifndef HIALGORITHM_BASE_H
#define HIALGORITHM_BASE_H

#include <cmath>
#include <cstddef>
using namespace std;

static auto _ = []() {
    srand(time(nullptr));
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    return nullptr;
}();

#endif  // HIALGORITHM_BASE_H
